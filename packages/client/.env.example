# Set these to the host's IP on whichever network you share with friends
# (Radmin VPN, Tailscale, ZeroTier, or a plain local network all work).
VITE_API_URL=http://<host-ip>:3001
VITE_WS_URL=ws://<host-ip>:3002
